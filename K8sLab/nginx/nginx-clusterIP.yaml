# You can access mongodb directly by typing url: (minikube ip): 30008
---
apiVersion: v1
kind: Service
metadata:
  name: mongo-nodeport
spec:
  type: ClusterIP
  selector:
    app: mongodb
  ports:
  - protocol: TCP
    port: 27017
    targetPort: 27017